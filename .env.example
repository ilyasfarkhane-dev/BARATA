# Optional: set a custom password for the portfolio dashboard (/dashboard).
# If not set, the default password is "admin".
# VITE_DASHBOARD_PASSWORD=your-secure-password

# Cloudinary (for project image uploads in the dashboard)
# 1. Cloud name: from Cloudinary console → API Keys (e.g. dqmcvusqd)
# 2. Upload preset: Console → Settings → Upload → Upload presets → Add upload preset
#    - Set "Signing Mode" to "Unsigned" so the frontend can upload without your API secret
#    - Copy the preset name here
VITE_CLOUDINARY_CLOUD_NAME=your_cloud_name
VITE_CLOUDINARY_UPLOAD_PRESET=your_unsigned_upload_preset
